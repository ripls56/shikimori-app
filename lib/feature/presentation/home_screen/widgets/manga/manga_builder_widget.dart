import 'package:flutter/material.dart';

class MangaScreenBuilder extends StatelessWidget {
  const MangaScreenBuilder({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('manga'),
    );
  }
}
